events {}  

http {  
    server {  
        listen 80;  
        server_name localhost;  

        location / {  
            root /usr/share/nginx/html; # Thư mục tệp HTML  
            index index.html; # File trang chủ  
        }  
    }  
}